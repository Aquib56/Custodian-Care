import 'package:flutter/material.dart';

class PlumbingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Plumbing Service'),
      ),
      body: Center(
        child: Text('This is the Plumbing Service Page'),
      ),
    );
  }
}
