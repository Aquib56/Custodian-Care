import 'package:flutter/material.dart';

class PaintingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Painting Service'),
      ),
      body: Center(
        child: Text('This is the Painting Service Page'),
      ),
    );
  }
}
