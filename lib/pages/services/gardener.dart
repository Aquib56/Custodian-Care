import 'package:flutter/material.dart';

class GardenerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Gardener Service'),
      ),
      body: Center(
        child: Text('This is the Gardener Service Page'),
      ),
    );
  }
}
