import 'package:flutter/material.dart';

class ACRepairPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('AC Repair Service'),
      ),
      body: Center(
        child: Text('This is the AC Repair Service Page'),
      ),
    );
  }
}
