import 'package:flutter/material.dart';

class PestControlPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pest Control Service'),
      ),
      body: Center(
        child: Text('This is the Pest Control Service Page'),
      ),
    );
  }
}
