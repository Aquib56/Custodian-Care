import 'package:flutter/material.dart';

class MaidPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Maid Service'),
      ),
      body: Center(
        child: Text('This is the Maid Service Page'),
      ),
    );
  }
}
