import 'package:flutter/material.dart';

class HairSalonPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Hair Salon Service'),
      ),
      body: Center(
        child: Text('This is the Hair Salon Service Page'),
      ),
    );
  }
}
